import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

// Mounts the React application into the Vite root element.
ReactDOM.createRoot(document.getElementById("root")).render(
  <App />
);
